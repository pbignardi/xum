# `xum` - another Tmux session manager
Yes, it's another sessionizer using `tmux` and `fzf`.

## Dependencies
- `python` (at least 3.13)
- `zoxide`
- `fzf`

## Installation
Using `pipx`
```bash
pipx install xum
```
